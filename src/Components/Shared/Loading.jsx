import loader from '../../Assets/images/loader.gif';

export default function Loading() {
    return (
        <p className='notFound'><img src={loader} alt='loader' className='loader' /></p>
    )
}
